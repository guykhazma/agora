# Python
.venv/
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/

# Node / frontend
node_modules/
frontend/dist/
frontend/.vite/
frontend/bun.lock

# Local data copies (generated by `cp -r ../data public/` for local dev)
frontend/public/data/
frontend/public/iceberg/
frontend/public/projects.json

# Derived site data — regenerated at deploy time by scripts/build_site_data.py
# from the canonical proposals.json. Never committed (keeps the repo lean).
data/*/index.json
data/*/feed.xml

# Local agent handoff notes (see HANDOFF.md header — local only)
HANDOFF.md

# Local secrets
.env
.env.local
*.env

# Claude Code
.claude/

# macOS
.DS_Store
